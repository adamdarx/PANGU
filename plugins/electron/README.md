# Electron plugin

The electron plugin advances passive electron entropy components and applies the selected heating
models through PANGU's passive-stage contract. It requires an active MHD fluid package.

```text
<plugins>
enabled = electron
```

The existing `<electrons>` block remains the plugin's model configuration namespace so restart and
input compatibility are preserved during migration.
