# Radiation transport plugin

This plugin implements radiation moment transport, geodesic angular grids, radiation-fluid
coupling, and the associated radiation test problems. It conflicts with `radiation_cooling`.
